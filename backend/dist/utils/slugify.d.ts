/**
 * Creates a URL-safe slug from a string.
 * Appends a short random suffix to prevent duplicates.
 */
export declare function createSlug(text: string, unique?: boolean): string;
//# sourceMappingURL=slugify.d.ts.map